document.addEventListener('DOMContentLoaded',()=>{const s=document.getElementById('searchBar');const games=[...document.querySelectorAll('.game')];const genres=[...document.querySelectorAll('.sidebar a')];function filter(){const q=s.value.toLowerCase();const active=document.querySelector('.sidebar a.active');const g=active?active.dataset.genre:'all';games.forEach(x=>{const title=x.dataset.title.toLowerCase();const gen=(x.dataset.genre||'').toLowerCase();const okTitle=title.includes(q);const okGenre=g==='all'||gen.includes(g);x.style.display=okTitle&&okGenre?'block':'none';});}s.addEventListener('input',filter);genres.forEach(a=>a.addEventListener('click',e=>{e.preventDefault();genres.forEach(x=>x.classList.remove('active'));a.classList.add('active');filter();}));genres[0].classList.add('active');filter();});